# PostgreSQL variant — use with: docker compose --profile pgsql up
# Then set DB_HOST=postgres in .env

DB_CONNECTION=pgsql
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=laravel_tenant_kit
DB_USERNAME=laravel
DB_PASSWORD=secret
